# Project-specific Python dependencies (provisioning and build tools).
#
# ESP-IDF's own Python packages (esptool, esp-idf-* tools) are managed by
# the IDF installation and should NOT be listed here. Inside the Docker
# container they are pre-installed in IDF's venv.
#
# Only list packages your project scripts directly import.

# Device provisioning / NVS generation
bitstring>=4.0,<5

# AWS IoT device onboarding
boto3>=1.26,<2
cryptography>=43,<45

# OTA certificate fetch script
requests>=2.28,<3
